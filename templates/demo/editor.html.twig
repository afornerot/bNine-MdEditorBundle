<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
    <title>EasyMDE Example</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
    <script src="{{ asset('bundles/bninemdeditor/bninemdeditor.js') }}"></script>
</head>
<body class="bg-light">
    <div class="container py-5">
        <h1 class="mb-4">EasyMDE Markdown Editor</h1>

        <div class="row">
            <div class="col col-md-6">
                {{ form_start(form) }}
                    <div class="mb-3">
                        {{ form_widget(form.markdown) }}
                    </div>
                    <div class="mb-3">
                        {{ form_row(form.submit) }}
                    </div>
                {{ form_end(form) }}      
            </div>
            <div class="col col-md-6">
                {{ dto.markdown|markdown_to_html }}
            </div>
        </div>
    </div>
</body>
</html>
